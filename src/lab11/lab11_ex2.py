# Генерация списка из 20 элементов
A = [int(input(f"Введите элемент {i + 1}: ")) for i in range(20)]

# Вычисление количества отрицательных элементов списка
count_negative = sum(1 for x in A if x < 0)

# Вывод результата
print("Количество отрицательных элементов в списке:", count_negative)